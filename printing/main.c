// To print to console we need the function printf
// printf() is defined in standard input output library
// We include the stdio.h i.e standard input output library, which allows us to use printf();
#include <stdio.h>

int main()
{
    /*
        printf("Format String", Argument1, Argument2, ...);
    */
    printf("Hello world\n");

    // int num = 0;
    float percentage = 80.016345;
    // char letter = 'a';
    // char arrayletters[] = {'a', 'b', 'c', 'd', 'e'};
    // char stringletters[] = "abcde";
    // long longNum = 1000;
    // double percentageDb = 80.23213;

    // // Print
    // printf("%i\n", num);
    // printf("%c\n", letter);
    // printf("%.2f\n", percentage);
    // printf("%s\n", arrayletters);
    // printf("%s\n", stringletters);
    // printf("%li\n", longNum);
    // printf("%lf\n", percentageDb);
    return 0;
}